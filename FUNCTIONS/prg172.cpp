#include<iostream>
#include "compare.hpp"

int main(){

    int maximum = max(134,56);
    std::cout << maximum << std::endl;

    int minimum = min(134,56);
    std::cout << minimum << std::endl;

    return 0;
}

